# Theodora Stavropoulos Real Estate Website
# Copy this file to .env and fill in your values

# Broker headshot (optional) — if AboutPreview image fails, set a working URL
# VITE_BROKER_HEADSHOT_URL=https://your-cdn.com/theodora-headshot.jpg

# Hero image refresh (optional) — for scripts/refresh-hero-images.mjs weekly cron
# UNSPLASH_ACCESS_KEY=your_key_from_unsplash.com/developers

# PostgreSQL Database (Neon)
DATABASE_URL=postgresql://username:password@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# JWT Secret for session cookies (generate with: openssl rand -hex 32)
JWT_SECRET=your-random-64-char-hex-string

# Node environment
NODE_ENV=development

# Optional: Google Maps (backend proxy - key never exposed, works for all domains)
# Get a key from https://console.cloud.google.com/apis/credentials
# Enable "Maps JavaScript API" in your Google Cloud project
GOOGLE_MAPS_API_KEY=

# Optional: Tavus AI Avatar (CVI) — conversational AI assistant for lead capture
# Get from https://platform.tavus.io — create Replica + Persona, then add IDs here
TAVUS_API_KEY=
TAVUS_REPLICA_ID=
TAVUS_PERSONA_ID=
# Callback URL for lead capture — must be publicly reachable (e.g. https://yoursite.com/api/webhooks/tavus)
# If set, Tavus will POST transcripts here when conversations end; leads are saved to inquiries
TAVUS_CALLBACK_URL=

# Centris.ca listings sync (via Apify — no public Centris API)
# Get token from https://console.apify.com/account/integrations
# Apify actor: ecomscrape/centris-property-search-scraper (~$20/mo + usage)
APIFY_TOKEN=

# Cron auth — Vercel sends this when invoking scheduled jobs; set in Vercel env vars
# Generate with: openssl rand -hex 32
CRON_SECRET=

# ElevenLabs Voice AI (nexrel.soshogle.com-style floating bubble)
# Option A - CRM: Set these when site is linked to a Website record in nexrel-crm
# NEXREL_CRM_URL=https://www.nexrel.soshogle.com
# NEXREL_WEBSITE_ID=website_id_from_crm
# WEBSITE_VOICE_CONFIG_SECRET=shared_secret_matching_crm
# WEBSITE_VOICE_LEAD_SECRET=shared_secret_for_lead_webhook
# Option B - Standalone: Use agent ID directly (no CRM lead push)
# NEXREL_ELEVENLABS_AGENT_ID=your_elevenlabs_conversational_agent_id

# Push leads to nexrel-crm (optional)
# NEXREL_CRM_LEAD_OWNER_ID=user_id_of_theodora_in_crm
# TAVUS_WEBHOOK_SECRET=shared_secret_for_webhook_auth
